<div class="field">
  <div class="info">
    <%= label unless label.blank? %>
    <%= content_tag(:p, tip, :class => 'tip') unless tip.blank? %>
  </div>
  <div class="input">
    <%= element %>
    <%= field_error(errors) unless errors.blank? %>
  </div>  
</div>
