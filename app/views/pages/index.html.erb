<% heading 'Pages' %>

<% toolbar_item link_to(glyph('add') + ' New page', new_resource_path()) %>
<% toolbar_item link_to('Organize', organize_resources_path()) %>

<ul id="pages">
  <%= render :partial => 'page', :collection => self.resource.children %>
</ul>