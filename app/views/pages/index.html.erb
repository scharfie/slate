<% toolbar_link glyph('add') + ' New page', new_resource_path() %>
<% toolbar_link glyph('organize') + ' Organize', organize_resources_path() %>

<ul id="pages">
  <%= render :partial => 'page', :collection => self.resource.children %>
</ul>